# BeastarsDl, a python script to download all Beastars chapters as pdfs
## Dependencies
  - `requests`
  - `PIL`
  - `PyPDF2`
## Usage
Just run it!  
It will make a file called `chapters` and download every Beastars manga to it!